#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_batch_core(ModuleInitializationLevel p_level);
void uninitialize_batch_core(ModuleInitializationLevel p_level);
